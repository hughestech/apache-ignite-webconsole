apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-ignite-webconsole.fullname" . }}
  labels:
    app: {{ include "apache-ignite-webconsole.name" . }}
    chart: {{ include "apache-ignite-webconsole.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "apache-ignite-webconsole.name" . }}
    release: {{ .Release.Name }}
